<!DOCTYPE html>
<html lang="en">
  
<%- include('../includes/head', {title: 'CD Detail | CD Shop'}) %>

<body>

  <%- include('../includes/header') %>

  <main>

    <h3> <%= cD.name %> </h3>

    <p> Description: <%= cD.description %> </p>

    <p> Price: <%= readablePrice(cD.priceInCents) %> </p>

    <% if (req.isAuthenticated()) { %>
      <a href="/shop/<%= cD.slug %>/edit">Edit inventory entry</a>
      <br>
      <br>
    <% } %>

    <% if (req.isAuthenticated()) { %>
      <a href="/shop/<%= cD.slug %>/delete">Delete inventory entry (be careful - this cannot be undone!)</a>
    <% } %>

  </main>
  <%- include('../includes/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</body>